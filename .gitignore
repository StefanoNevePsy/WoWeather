*.iml
.gradle/
/local.properties
/.idea/
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
/app/build/


# The release APK is committed under dist/ on purpose: it is the deliverable,
# and a build environment is not always available to regenerate it.
!/dist/
